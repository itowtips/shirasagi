<%#= render file: "cms/agents/pages/page/index" %>

<!-- Member::Addon::Photo::Spot -->

<% map_points = @cur_page.member_photos.and_public.map(&:map_points).select(&:present?).flatten %>
<% if map_points.present? %>
<%= render_map "#map-canvas", markers: map_points, site: @cur_site %>
<div id="map-canvas" style="width: 100%; height: 400px;"></div>
<% end %>
