<%= form_tag '', class: "search", method: :get, enforce_utf8: false do %>
  <%= select_tag :g, options_for_select(@cur_groups.map { |g| [g.name, g.id] }, params[:g]), include_blank: true %>
  <input type="submit" value="<%= t('views.button.search') %>" class="btn" />
<% end %>
