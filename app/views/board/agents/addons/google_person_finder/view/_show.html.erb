<% return unless @cur_node.gpf_enabled? %>

<dl class="column">
  <dt><%= I18n.t('board.views.gpf') %></dt>
  <dd><%= link_to('Google Person Finder を表示する', @item.gpf_url.to_s, target: :_blank) %></dd>
</dl>
