<% if @merged_map.present? %>
  <% @cur_page = @merged_map %>
  <%= render file: "map/agents/addons/page/view/index", locals: { no_header: true } %>
<% end %>

<div class="tourism-pages pages">
  <%= render_page_list %>
</div>

<%= paginate @items if @items.try(:current_page) %>
