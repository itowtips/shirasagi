<% return render(text: "") unless @item.allowed?(:release, @cur_user, site: @cur_site) %>
<% return "" if @item.categories.blank? %>

<dl class="see">
  <dd class="wide"><%= @item.send(:categories).pluck(:name).join(", ") %></dd>
</dl>
