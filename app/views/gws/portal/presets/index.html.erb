<% @index_title = proc do |item| %>
  <%= link_to item.name, gws_portal_preset_layouts_path(preset: item.id), class: "title" %>
<% end %>

<% @index_meta = proc do |item| %>
  <span class="id">#<%= item.id %></span>
  <span class="datetime"><%= ss_time_tag item.updated %></span>
  <span class="portal_type"><%= item.label :portal_type %></span>
  <span class="order"><%= item.order %></span>
<% end %>

<%= render template: "gws/crud/index" %>
