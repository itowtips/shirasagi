<table class="circle-nodes">
  <thead>
    <tr>
      <th><%= Circle::Node::Page.t :name %></th>
      <th><%= Circle::Node::Page.t :address %></th>
    </tr>
  </thead>
  <tbody>
    <% @items.each do |item| %>
    <tr>
      <td><%= link_to item.name, item.url %></td>
      <td><%= item.address %></td>
    </tr>
    <% end %>
  </tbody>
</table>

<%= paginate @items if @items.try(:current_page) %>
