<%= node_navi do %>

<nav class="sub-menu">
  <header><h1><%= t "circle.circle" %></h1></header>
  <% if params[:action] =~ /show|edit/ %>
    <div>
      <%= link_to :"circle.image", circle_images_path(cid: @item.id), class: "icon-page" %>
    </div>
  <% else %>
    <div>
      <%= link_to :"circle.page", circle_pages_path, class: "icon-node" %>
    </div>
  <% end %>
</nav>

<% end %>
