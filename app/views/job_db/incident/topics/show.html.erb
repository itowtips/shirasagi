<div class="job_db-incident job_db-incident-thread">
  <div class="main">
    <article class="topic">
      <header>
        <h2>
          <span class="name"><%= @item.name %></span>
        </h2>
        <nav class="meta">
          <span class="datetime"><%= tryb { @item.updated.strftime("%Y/%m/%d %H:%M") } %></span>
        </nav>
      </header>

      <div class="body markdown-body">
        <%= @item.html %>
      </div>

      <% if @item.files.present? %>
        <div class="files">
          <% @item.files.each do |file| %>
            <img src="/assets/img/gws/ic-file.png" alt="" />
            <%= link_to file.humanized_name, file.url, class: "icon-#{file.extname}", target: '_blank' %>
          <% end %>
        </div>
      <% end %>

      <% if @model.allowed?(:edit, @cur_user) %>
        <div class="menu">
          <%= link_to(t('gws/board.links.comment'), new_job_db_incident_topic_comment_path(topic_id: @item.id), class: "btn primary") %>
        </div>
      <% end %>
    </article>

    <% if @item.children.present? %>
      <div class="comments">
        <%= render partial: 'comment', collection: @item.descendants, locals: { parent: @item, recursion: false } %>
      </div>
    <% end %>
  </div>
  <div class="meta">
    <article class="topic">
      <header>
        <h2>
          <span class="name">属性</span>
        </h2>
      </header>
      <dl class="see attributes">
        <dt>状態</dt>
        <dd>担当者未定 | 担当決定 | 解決 | 差し戻し | ...</dd>

        <dt>最終更新日</dt>
        <dd><%= tryb { @item.descendants_updated.strftime("%Y/%m/%d %H:%M") } %></dd>

        <dt>担当者</dt>
        <dd><a href="#">櫻井翔</a></dd>

        <dt>問い合わせ</dt>
        <dd><a href="#">木村拓哉</a></dd>

        <dt>問い合わせ日</dt>
        <dd>2016/12/25 16:04</dd>

        <dt>求人情報</dt>
        <dd><a href="#">大企業のすごい求人</a></dd>

        <dt>Web フォーム</dt>
        <dd><a href="#">xxxxx</a></dd>

        <dt>イベント申し込み</dt>
        <dd><a href="#">xxxxx</a></dd>

        <dt><%= JobDb::Incident::Category.model_name.human %></dt>
        <% @item.categories.allow(:read, @cur_user).each do |category| %>
            <dd><%= link_to category.trailing_name, job_db_incident_topics_path(category: category.id), class: "job_db-category-label", style: category_label_css(category) %></dd>
        <% end %>
      </dl>
    </article>
  </div>
</div>
