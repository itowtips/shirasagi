<dl class="see">
  <dt><%= @model.t :name %></dt>
  <dd><%= @item.name %></dd>

  <dt><%= @model.t :poster_template %></dt>
  <dd><%= @item.poster_template %></dd>

  <dt><%= @model.t :send_at %></dt>
  <dd><%= @item.send_at.strftime("%Y/%m/%d %H:%M") if @item.send_at %></dd>

  <%= link_to "配信する", { action: :deliver } %>
</dl>
