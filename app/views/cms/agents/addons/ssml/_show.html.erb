<dl class="see">
  <dd class="wide">
    <% if @item.ssml.present? %>
    <%= code_editor "#mod-cms-ssml", mode: :html, readonly: true %>
    <%= text_area_tag "mod-cms-ssml", @item.ssml, style: "height: 400px" %>
    <% end %>
  </dd>
</dl>
