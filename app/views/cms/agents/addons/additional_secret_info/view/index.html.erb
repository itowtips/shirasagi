<% return "" unless @cur_user %>

<% @item = @cur_page || @cur_node %>
<% if @item.additional_secret_info.present? %>
<dl class="see additional-secret-info">
  <% @item.additional_secret_info.each do |info| %>
  <dt><%=br info[:field] %></dt>
  <dd><%=br info[:value] %></dd>
  <% end %>
</dl>
<% end %>
