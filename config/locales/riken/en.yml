en:
  riken:
    ldap:
      main: Riken LDAP
      setting: Setting
      group: Group
      user: User
      connection_setting: Connection Settings
      group_condition: Group Search Conditions
      user_condition: User Search Conditions
      roles: Default Roles
      custom_groups: Custom Groups
      test_connection: Test Connection
      test_search: Test Search
      max_custom_groups_notice: (<= %{count} items)
    shibboleth:
      login_failed:
        head: Failed to login
        body: "Failed to login. Please consult your system administrator."

  modules:
    riken: Riken
    addons:
      riken/shibboleth_setting: Shibboleth Settings

  mongoid:
    models:
      riken/auth/shibboleth: Shibboleth Auth
    attributes:
      riken/addon/shibboleth_setting:
        login_url: Login URL

  tooltip:
    riken/addon/shibboleth_setting:
      login_url:
        - Set the login url for Shibboleth login

  job:
    models:
      riken/ldap/import_job: RIKEN LDAP Import
