en:
  riken:
    ldap:
      main: Riken LDAP
      setting: Setting
      group: Group
      user: User
      connection_setting: Connection Settings
      group_condition: Group Search Conditions
      user_condition: User Search Conditions
      roles: Default Roles
      custom_groups: Custom Groups
      test_connection: Test Connection
      test_search: Test Search
      max_custom_groups_notice: (<= %{count} items)
      custom_group_conditions_name: Custom Group Name
      custom_group_conditions_dn: DN
      custom_group_conditions_filter: Filter
    ms365:
      main: Microsoft365
      setting: Setting
      room_list: Room List
      room: Room
      event: Event
    shibboleth:
      login_failed:
        head: Failed to login
        body: "Failed to login. Please consult your system administrator."

  modules:
    riken: Riken
    addons:
      riken/shibboleth_setting: Shibboleth Settings

  mongoid:
    models:
      riken/auth/shibboleth: Shibboleth Auth
    attributes:
      riken/addon/shibboleth_setting:
        login_url: Login URL
      riken/ldap/group_setting:
        riken_ldap_url: LDAP URL
        riken_ldap_bind_dn: LDAP Account
        riken_ldap_bind_password: Password
        riken_ldap_group_dn: Group DN
        riken_ldap_group_filter: Group Filter
        riken_ldap_user_dn: User DN
        riken_ldap_user_filter: User Filter
        riken_ldap_sys_role_ids: System Permissions
        riken_ldap_gws_role_ids: GWS Permissions
        riken_ldap_custom_group_conditions: Custom Group Conditions
      riken/ms365/group_setting:
        riken_ms365_tenant_id: Tenant ID
        riken_ms365_client_id: Client ID
        riken_ms365_client_secret: Client Secret

  tooltip:
    riken/addon/shibboleth_setting:
      login_url:
        - Set the login url for Shibboleth login

  job:
    models:
      riken/ldap/import_job: RIKEN LDAP Import
